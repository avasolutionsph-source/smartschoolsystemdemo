[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so /login, /admin/*, /student/*, etc. resolve to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
